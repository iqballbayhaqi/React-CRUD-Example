import React, { Component } from 'react';
import { render } from '@testing-library/react';

export default Edit extends Component {
    render() {
        return(
            <div>
                <p>Welcome to Edit Component !!</p>
            </div>
        )
    }
}